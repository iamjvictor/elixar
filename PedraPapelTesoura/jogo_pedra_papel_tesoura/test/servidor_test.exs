defmodule JogoPedraPapelTesoura.ServidorTest do
  use ExUnit.Case

  test "testa a resposta do servidor" do
    # Aqui você pode escrever testes para o servidor, como testar se ele retorna a resposta correta para uma escolha do jogador.
  end
end
